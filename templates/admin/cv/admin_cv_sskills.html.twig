
<div class="ajax_cv_view mx-auto">
    <h2 class="text-center section_title_litle">
        Mes SoftSkills :
    </h2>
    <div class="mt-3 mb-3 text-center">
    <a href="{{ path('add_sskill') }}" class="mt-3 mb-3"><input class="btn btn-primary" type="button" value="Ajouter"></a>
    </div>
    <div class="row align-items-center p-auto">
        <table class="table table-sriped col-6 mx-auto">
            <thead>
                <tr>
                    <th>Softskills :</th>
                </tr>
            </thead>
            <tbody>
                {% for softskill in softskills %}
                    <tr>
                        <th>                
                            <div class="soft_skills m-auto badge {{ random(['badge-primary', 'badge-success', 'badge-danger', 'badge-warning', 'badge-info']) }}">{{ softskill.name }}</div>
                        </th>
                        <th>
                            <a href="{{ path('update_sskill', {'id': softskill.id }) }}" class="admin_update_diplome"><input type="button" value="MODIFIER" class="btn btn-primary"></a> 
                        </th>
                        <th>
                            <a href="{{ path('delete_sskill', {'id': softskill.id }) }}" class="admin_delete_diplome"><input type="button" value="SUPPRIMER" class="btn btn-danger"></a> 
                        </th>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>







