<div class="ajax_cv_view">
    <h2 class="text-center section_title_litle">
        Mes expériences
    </h2>
    <div class="row align-items-center p-auto">
        <a href="{{ path('add_xp') }}" class="mt-3 mb-3"><input class="btn btn-primary" type="button" value="Ajouter"></a>
        <table class="table table-sriped">
            <thead>
                <tr>
                    <th>Année(s)</th>
                    <th>Expérience</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% for xp in xps %}
                    <tr class="xp_id{{ xp.id }}">
                        <th>{{ xp.year }}</th>
                        <th>{{ xp.name }}</th>
                        <th>
                            <a href="{{ path('update_xp', {'id': xp.id }) }}" class="admin_update_xp"><input type="button" value="MODIFIER" class="btn btn-primary"></a> 
                        </th>
                        <th>
                            <a href="{{ path('delete_xp', {'id': xp.id }) }}" class="admin_delete_xp"><input type="button" value="SUPPRIMER" class="btn btn-danger"></a> 
                        </th>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>







